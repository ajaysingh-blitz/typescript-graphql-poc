{ "data" :
 {
    "type": "container",
    "args": {
      "height": 230,
      "child": {
        "type": "list_view",
        "args": {
          "scrollDirection": "horizontal",
          "children": "${for_each(entries['values'][0]['entities'], 'testimonial_template')}"
        }
      }
    }
 }
}